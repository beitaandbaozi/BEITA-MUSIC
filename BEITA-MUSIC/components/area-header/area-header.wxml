<view class="area-header">
  <view class="title">{{title}}</view>
  <block wx:if="{{hasMore}}">
    <view class="more" bindtap="handleToMore">
      <text class="text">更多</text>
      <van-icon name="arrow" />
    </view>
  </block>
</view>